<!DOCTYPE html>
<html lang="ch-ZN">

<head>
    <title>主页-组管理</title>
    <meta charset="utf8">
    <meta name="viewport" content="width=device-width, initial-scale=1, user-scalable=no">
    <link rel="stylesheet" type="text/css" href="/src/group/group.css">
</head>

<body>
    <div class="container">
        <!--头部-->
        <header>
            <nav>
                <ul class="nav nav-decoration">
                    <% modules.forEach(function(module){ %>
                        <li class="<%=module.active=='active'?'active':''%>">
                            <a href="<%=module.href%>">
                                <%=module.name%>
                            </a>
                        </li>
                        <% }) %>
                </ul>
                <div class="user-oprt-container">
                    <a class="user-oprt" id="refreshUser"><i class="glyphicon glyphicon-refresh"></i></a>
                    <a class="user-oprt" id="addUser"><i class="glyphicon glyphicon-plus"></i></a>
                    <a class="user-oprt" id="deleteUser"><i class="glyphicon glyphicon-remove"></i></a>
                    <a class="user-oprt" id="saveUser"> <i class="glyphicon glyphicon-ok"></i> </a>
                </div>
            </nav>
        </header>
        <!--/头部结束-->


        <!--主要部分-->
        <main>
            <div class="table-responsive">
                <table class="table table-bordered text-center" id="tblUsers">
                    <thead>
                        <tr class="text-center">
                            <td>行号</td>
                            <td><input type="checkbox" id="checkAllUsers"></td>
                            <td>用户名</td>
                            <td>密码</td>
                            <td>权限等级</td>
                            <td>状态</td>
                        </tr>
                    </thead>
                    <tbody>
                        <!--这里存放主要的数据-->
                    </tbody>
                </table>
            </div>
        </main>
        <!--/主要部分结束-->

    </div>

    <script src="/lib/requirejs/require.js" data-main="/src/group/group.js"></script>
</body>

</html>